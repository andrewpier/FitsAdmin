//
//  FitsTests.swift
//  FitsTests
//
//  Created by Sophia Gebert on 1/20/16.
//  Copyright © 2016 Urban Fish Studio. All rights reserved.
//
/*
import XCTest
@testable import Fits

class FitsTests: XCTestCase {
    var viewController: NewLoginViewController!
    var RegisterTest =  NewRegisterViewController()
    override func setUp() {
        super.setUp()
        // Put setup code here. This method is called before the invocation of each test method in the class.
        
    }
    
    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }
    
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        XCTAssert(true, "Pass")
    }
    func testImageNameRetakeView(){
       
        let imageName = RetakeViewController().getImageName()
        XCTAssertNotEqual(imageName,"terriblywrong.JPEG")
    }
    func testPerformanceExample() {
        // This is an example of a performance test case.
        self.measureBlock {
            // Put the code you want to measure the time of here.
        }
        XCTAssert(true, "Pass")
    }
    
    func testIsValidEmailFunc() {
        var login = RegisterTest.isValidEmail("andrew@pier.com")
        XCTAssertEqual(login, true, "a valid email")
        login = RegisterTest.isValidEmail("aasdf")
        XCTAssertEqual(login, false, "invalid email")
    }
    
    func testIsValidPasswordFunc() {
        var login = RegisterTest.isValidPass("Pass1234")
        XCTAssertEqual(login, true, "a valid pass")
        login = RegisterTest.isValidEmail("aasdf")
        XCTAssertEqual(login, false, "invalid pass")
    }
}*/
